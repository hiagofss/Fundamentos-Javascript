// Let é uma variavel que pode ser re-assinada
let nome = 'Super Man';
let idade = 15;
let poder = 'Super força';
nome = 'Spider Man';

// Const usado para valores que não se alteram
const ativo = true;

// Var não deve ser usada mais
var pais = 'Brasil';

console.log(
  `
  Nome: ${nome}
  Idade: ${idade}
  Poder: ${poder}
  `
);
